server {
    listen 443 ssl;
    server_name archiv.zfk-bh.de;

    client_max_body_size 2G;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme; # VERY IMPORTANT
    }

    ssl_certificate /etc/letsencrypt/live/archiv.zfk-bh.de/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/archiv.zfk-bh.de/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
}

server {
    listen 80;
    listen [::]:80;
    server_name archiv.zfk-bh.de;

    return 301 https://$host$request_uri;
}